/**
 * Sure 主题背景层 — 复用 default 主题的背景实现（图片 + 滚动渐变 + 模糊）
 * sure 主题的配置结构（homeImage/globalImage/light/dark/backdropBlur/transitionDuration）
 * 与 default 完全一致。
 */
export { BackgroundLayer } from "@/features/theme/themes/default/components/background-layer";
